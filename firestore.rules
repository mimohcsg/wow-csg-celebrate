rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // App data is only accessed via Admin SDK in Cloud Functions.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
